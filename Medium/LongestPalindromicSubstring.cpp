#include <bits/stdc++.h>

using namespace std;
 
    int maxArea(vector<int>& height) {
        
             for (int i=0; i<height.size(); i++){

                cout<<"numbers:"<<height[i]<<endl;
             }

    }
int main(){
     
    vector<int> numbers = {2,3,4,5,6,8,5,4}; 
   maxArea(numbers);

    return 0;
}